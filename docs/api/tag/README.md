# Tag

Core contract file: `packages/core/src/tag-contract.ts`.

Core contract: `TagContract`。

| Name | Type | Default |
| --- | --- | --- |
| `tone` | `'neutral' \| 'success' \| 'warning' \| 'danger' \| 'info'` | `'neutral'` |

React 使用 children，Vue 使用默认 slot，Web Component 使用 `<h-tag>内容</h-tag>`。

